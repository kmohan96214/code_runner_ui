/* eslint-disable */
import React from 'react'
import Header from 'Lib/Header'
import { Pagetitle } from 'Lib/Subheader'
import Sticky from 'Lib/Sticky'

import SearchContainer from 'Containers/Hotels/Search'

const Search = props => {
  const pageTitle = 'Find and book hotels around the world'
  return (
    <div>
      <Sticky position="top" renderChild={() => {
        return (
          <>
            <Header menu={true} />
            <Pagetitle title={pageTitle} backButton={false} isHomeHeader={true} />
          </>
        )
      }}>
        <Header menu={true} />
        <Pagetitle title={pageTitle} backButton={false} />
      </Sticky>
      <SearchContainer />
    </div>
  )
}

export default Search
