import React from 'react'

import { Pagetitle } from 'Lib/Subheader'
import Header from 'Lib/Header'

import HomeComponent from 'Components/Home'
import Footer from 'Containers/Footer'
import Sticky from 'Lib/Sticky'

const pageTitle = 'Making travel simple'

const Home = () => {
  return (
    <div>
      <Sticky>
        <Header menu={true} />
        <Pagetitle title={pageTitle} backButton={false} />
      </Sticky>
      <HomeComponent />
      <Footer />
    </div>
  )
}

Home.displayName = 'Cleartrip(Home)'

export default Home
