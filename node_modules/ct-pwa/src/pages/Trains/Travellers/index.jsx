import React from 'react'
import PropTypes from 'prop-types'

import Header from 'Lib/Header'
import { Pagetitle } from 'Lib/Subheader'
import Sticky from 'Lib/Sticky'

import TravellersContainer from 'Containers/Trains/Travellers'

const Travellers = props => {
  const pageTitle = 'Traveller and Contact Information'
  return (
    <div>
      <Sticky>
        <Header menu={true} />
        <Pagetitle title={pageTitle} />
      </Sticky>
      <TravellersContainer itineraryId={props.params.id} />
    </div>
  )
}

Travellers.propTypes = {
  params: PropTypes.objectOf(PropTypes.any).isRequired
}

Travellers.displayName = 'Trains(TravellersPage)'

export default Travellers
