import React from 'react'

import { Pagetitle } from 'Lib/Subheader'
import Header from 'Lib/Header'
import Sticky from 'Lib/Sticky'

import ReviewContainer from 'Containers/Trains/Review'

const Review = props => {
  const pageTitle = 'Review Itinerary'
  return (
    <div>
      <Sticky>
        <Header menu={true} />
        <Pagetitle title={pageTitle} />
      </Sticky>
      <ReviewContainer />
    </div>
  )
}

Review.displayName = 'Trains(ReviewPage)'

export default Review
