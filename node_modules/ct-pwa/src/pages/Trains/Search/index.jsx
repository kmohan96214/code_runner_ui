import React from 'react'

import Header from 'Lib/Header'
import { Pagetitle } from 'Lib/Subheader'

import SearchContainer from 'Containers/Trains/Search'

const Search = props => {
  const pageTitle = 'Search Indian Rlwys. & IRCTC trains'
  return (
    <div>
      <Header menu={true} />
      <Pagetitle title={pageTitle} backButton={false} />
      <SearchContainer />
    </div>
  )
}

Search.displayName = 'Trains(SearchPage)'

export default Search
