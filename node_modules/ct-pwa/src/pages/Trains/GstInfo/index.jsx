import React from 'react'

import { Pagetitle } from 'Lib/Subheader'
import Sticky from 'Lib/Sticky'
import Header from 'Lib/Header'

import GstInfoContainer from 'Containers/Trains/TravellersDetails/gst'

const GstInfo = props => {
  const pageTitle = 'Travellers GST Details'
  return (
    <div>
      <Sticky>
        <Header menu={true} />
        <Pagetitle title={pageTitle} />
      </Sticky>
      <GstInfoContainer />
    </div>
  )
}

GstInfo.displayName = 'Trains(GstInfoPage)'

export default GstInfo
