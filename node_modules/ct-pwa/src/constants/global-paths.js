import { appRoutePrefix } from './'

export const CT_USER_LOGIN = `${appRoutePrefix}/login`

export const CT_USER_REGISTER = `${appRoutePrefix}/register`

export const SEARCH_PAGE = type => `/m/${type}/search`
