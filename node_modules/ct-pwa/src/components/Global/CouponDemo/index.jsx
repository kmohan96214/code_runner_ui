import React from 'react'
import { Pagetitle } from 'Lib/Subheader'

import { List, ListItem } from 'Lib/List'
import { SectionHeader } from 'Lib/Text'
import Overlay from 'Lib/Overlay'
import CashbackModal from 'Lib/CashbackModal'
import Input from 'Lib/Form/Input'

const onActionClick = () => {
  console.log('Action Clicked') // eslint-disable-line
}

const ModalDemo = props => {
  return (
    <div>
      <Pagetitle title="Coupon Demo" />

      <SectionHeader>Apply Coupon Code</SectionHeader>

      <List className="mt-0">
        <Input
          type="text"
          label="Apply Coupon"
          labelClassName="fs-15 c-black-90 flex-fix"
          inputClassName="fs-15 c-black-90"
          wrapper={ListItem}
          wrapperClassName="h-48 flex flex-middle"
          name="coupon"
          actionLabel="Apply"
          actionClassName="fs-15 c-blue"
          onActionClick={onActionClick}
        />
      </List>

      <Overlay>
        <CashbackModal />
      </Overlay>
    </div>
  )
}

export default ModalDemo
